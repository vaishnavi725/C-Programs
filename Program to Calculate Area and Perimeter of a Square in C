#include <stdio.h>

int main() {
    int side;
    int area, perimeter;
    scanf("%d", &side);
    area = side * side;
    perimeter = 4 * side;
    printf("Area: %d\n", area);
    printf("Perimeter: %d\n", perimeter);
    return 0;
}
