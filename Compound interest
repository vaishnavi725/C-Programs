#include <stdio.h>
#include <math.h>

int main(){
    double p, r, t, ci;
    if(scanf("%lf %lf %lf",&p,&r,&t)!=3) return 0;
    ci = p * pow((1 + r/100), t) - p;
    printf("%.2f\n", ci);
    return 0;
}
