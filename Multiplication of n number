#include<stdio.h>
int main()
{
	int n,i,r,num;
	printf("n:");
	scanf("%d",&n);
		printf("num:");
	scanf("%d",&num);
	for(i=0;i<=n;i++)
	{
		r=num*i;
		printf("%d*%d=%d",num,i,r);
			printf("\n");
	}

	
}
